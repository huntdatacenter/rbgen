import os, glob
def build( bld ):
	pkg_sources = [ 'NAMESPACE', 'DESCRIPTION', 'src/RcppExports.cpp', 'src/Makevars', 'R/RcppExports.R', 'src/load.cpp' ]
	for source in pkg_sources:
		bld( rule = 'cp ${SRC} ${TGT}', source = 'package/' + source, target = 'rbgen/' + source, always = True )