def build( bld ):
	sources = [ 'NAMESPACE', 'DESCRIPTION', 'src/RcppExports.cpp', 'src/Makevars', 'R/RcppExports.R', 'src/load.cpp' ]
	for source in sources:
		bld( rule = 'cp ${SRC} ${TGT}', source = 'package/' + source, target = 'rbgen/' + source, always = True )
